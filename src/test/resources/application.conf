me.khazad-dum {
  greeting = "Welcome to Moria"

  akka {
    http {
      host = "localhost"
      port = 9080
    }
  }

  frdm {
    h2 {
      default {
        url = "jdbc:h2:mem:usermodel;AUTOCOMMIT=ON;DATABASE_TO_UPPER=FALSE;DB_CLOSE_DELAY=-1"
        driver = "org.h2.Driver"
        connectionPool = disabled
        keepAliveConnection = true
      }
    }
  }
}